# BE-HPG requirements
# NOTE: Google Colab already ships torch/torchvision built against its CUDA runtime —
# do NOT reinstall them there. For the LOCAL CPU smoke-test environment, install the
# CPU build of torch separately (see README "Local smoke environment").

# --- Deep learning ---
torch>=2.0
torchvision>=0.15
timm>=0.9.12          # mobilevit_s, deit_tiny_patch16_224, resnet50 (ImageNet-1K pretrained)

# --- Numerics / ML ---
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3     # MiniBatchKMeans (hard prototypes)
scikit-image>=0.21    # Sobel boundary, morphology helpers

# --- Medical imaging I/O & metrics ---
nibabel>=5.1          # read MSD NIfTI volumes
medpy>=0.4.0          # medpy.metric.binary.hd95 / asd (surface metrics)

# --- Data / config / viz / progress ---
pandas>=2.0           # slice<->volume manifest CSV
pyyaml>=6.0           # configs (single source of truth)
matplotlib>=3.7       # qualitative overlays, figures
tqdm>=4.65

# --- Testing (CPU smoke-test gate) ---
pytest>=7.4

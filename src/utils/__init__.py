"""Utilities (Phase 2 / Phase 6).

Planned modules:
  config.py     YAML loader with `base:` deep-merge + CLI overrides
  seed.py       global seeding (python/numpy/torch) + deterministic flags
  aggregate.py  collate results JSON -> mean+/-std comparison & ablation tables
  figures.py    qualitative montages, BE-HPG pipeline graphical abstract
"""
